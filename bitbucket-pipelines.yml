definitions:
  caches:
    duct: .duct

pipelines:
  default:
    - step:
        name: Duct
        image: golang:1.26
        caches:
          - duct
        script:
          - "[ -d .duct ] || git clone --branch main --depth 1 https://bitbucket.org/nmvinicius/duct.git .duct"
          - "chmod +x .duct/bin/duct"
          - "sh .duct/bin/duct run"
run-duct:
  image: alpine/git:latest
  cache:
    key: duct
    paths: [.duct/]
  before_script:
    - "[ -d .duct ] || git clone --branch stable --depth 1 https://gitlab.com/nmvinicius/duct.git .duct"
  script:
    - sh .duct/bin/duct run